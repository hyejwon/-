#bfs
#%%
import numpy as np
#%%
def bfs(graph, start):
    graph = np.array(graph)
    start = np.array(start)
    queue = [start]
    
    return graph, start
def dfs(graph, start):
    graph = np.array(graph)
    start = np.array(start)
    stack = [start]
    return graph, start
def dp(graph, start):
    graph = np.array(graph)
    start = np.array(start)
    return graph, start

# 수정
